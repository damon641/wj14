module.exports = {
    RackPercent:2,
    RoomExpireHours:1,
    RoomExpireHoursCheckCronRepeatTime : 60000,

    waitAfterRoundComplete: 2000,
    waitAfterGameReset:1000,
    waitBeforeGameReset:4000,
    Regular:20000,
    Fast:10000,
    waitBeforeGameStart: 10, //second

    // Setting Default
    turnTime : 10,
    extraTime : 10,
    playerDropPoint : 80, // Player Drop Point When Drop
    playerDropedRoundCount : 3, // When Player defult 3 turn default then drop player
    pointsValue : 20, // 
}